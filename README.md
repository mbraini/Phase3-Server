## 📦 Requirements & Dependencies

To ensure this project runs smoothly, make sure you have included the following dependencies for **Kotlin**:

### 🛠️ Gradle (Kotlin DSL - `build.gradle.kts`)

```kotlin
dependencies {
    implementation("com.google.code.gson:gson:2.10.1")
    
    implementation("org.json:json:20231018")
}
